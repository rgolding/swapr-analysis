
class CalibrationAssignment:

	def __init__(self, url, t = None):
		self.url = url
		self.t = t
		self.rating = {}
		self.construct()

	def construct(self):
		key = 'itemIndex'
		value = 
	def instructorRating(self, r):
		self.expert = r

	def studentRating(self, r):
		if (self.studentRating == []):
			self.studentRating


class CalibrationItem:

	def __init__(self, index, rating = None):
		self.index = index
		self.rating = rating

